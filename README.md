# Most popular algorithms written in JavaScript

To run the scripts use the node command in the terminal:

```
node [file_name].js
```

## Web interface

Open `index.html` in a browser or start the provided server:

```
node server.js
```

Then visit `http://localhost:3000` to run algorithms interactively.

